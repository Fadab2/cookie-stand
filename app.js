let Seattle = {
    minHours: 23,
    maxHours: 65,
    custPerHr: function () {
        Math.floor(Math.random() * 10)
    }
}

let tokyo = {
    minHours: 3,
    maxHours: 24,
    custPerHr: function () {
        Math.floor(Math.random() * 10)
    }
}

let dubai = {
    minHours: 11,
    maxHours: 38,
    custPerHr: function () {
        Math.floor(Math.random() * 10)
    }
}

let paris = {
    minHours: 20,
    maxHours: 38,
    custPerHr: function () {
        Math.floor(Math.random() * 10)
    }
}

let lima = {
    minHours: 2,
    maxHours: 16,
    custPerHr: function () {
        Math.floor(Math.random() * 10)
    }
}
